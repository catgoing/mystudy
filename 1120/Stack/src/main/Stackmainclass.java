package main;

public class Stackmainclass {

	public static void main(String[] args) {
		
		/*
		 	Stack: First in last out
		 	
		 */
		
		
		StackClass stac = new StackClass(5);
		
		
		
		String str = "one";
		stac.push(str);
		
		
		/*
		String pickStr = (String)stac.pick(); // 캐스트..? 왜?
		System.out.println("top object: " + pickStr); 
		
		str = "two";
		stac.push(str);
		
		pickStr = (String)stac.pick(); // 캐스트..? 왜?
		System.out.println("top object: " + pickStr); 
		
		String popStr = (String)stac.pop();
		System.out.println("삭제된 값: " + popStr);
		
		pickStr = (String)stac.pick(); // 캐스트..? 왜?
		System.out.println("top object: " + pickStr); 
		
		str = "3";
		stac.push(str);
		str = "4";
		stac.push(str);
		str = "5";
		stac.push(str);
		
		str = "6";
		stac.push(str);
		
		str = "7";
		stac.push(str);
		
		str = "8";
		stac.push(str);
		pickStr = (String)stac.pick(); // 캐스트..? 왜?
		System.out.println("top object: " + pickStr);
		
		popStr = (String)stac.pop();
		System.out.println("삭제된 값: " + popStr);
		popStr = (String)stac.pop();
		System.out.println("삭제된 값: " + popStr);
		popStr = (String)stac.pop();
		System.out.println("삭제된 값: " + popStr);
		popStr = (String)stac.pop();
		System.out.println("삭제된 값: " + popStr);
		popStr = (String)stac.pop();
		System.out.println("삭제된 값: " + popStr);
		popStr = (String)stac.pop();
		System.out.println("삭제된 값: " + popStr);
		*/
		
		
		/*
		Integer i = 111;
		stac.push(i);
		
		Integer t = (Integer)stac.pick(); // 캐스트..? 왜?
		System.out.println("top object: " + t);
		
		*/
		
		
		
		
		
	}

}
